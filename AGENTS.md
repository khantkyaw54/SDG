# プロジェクト開発ガイドライン (AGENTS.md)

本ドキュメントは、「地元の人だから知っている、個人店と出会えるアプリ」のグループ開発における共通ルールおよび開発ガイドラインです。開発およびAIエージェントによるコード生成・改修時は、本規約を遵守してください。

---

## 1. プロジェクト概要

* **プロダクト名 / コンセプト**: 地元の人だから知っている、個人店と出会えるアプリ
* **概要**: 地域にある個人経営のお店を探したり、実際に利用した地元の人がおすすめ情報を投稿・共有できるWebアプリケーション。大手グルメサイトやSNSでは見つけにくい、地域に根付いた小さなお店や知る人ぞ知る名店を発掘できる。
* **ターゲット層**:
  * **メイン**: 地元住民（日常的に街を利用・探索する人）
  * **サブ**: 観光客（ディープなローカル体験を求める人）
* **対応デバイス**: **スマホ中心（モバイルファースト）**

---

## 2. デザインシステム & UI設計基準

### 2.1 カラーパレット
個人店のぬくもり、親しみやすさ、街歩きのワクワク感を意識した配色とします。

| 用途 | カラー名称 | カラーコード | 適用例 |
| :--- | :--- | :--- | :--- |
| **メインカラー** | テラコッタ / ウォームオレンジ | `#E06A3B` | 主要ボタン、アクティブタブ、ヘッダーアクセント |
| **サブカラー（背景）** | ウォームホワイト / 生成り色 | `#FAF7F2` | ページ全体背景、カード背景 |
| **サブカラー（テキスト）** | ディープダーク | `#2B2D42` | 見出し・本文テキスト、引き締め要素 |
| **アクセントカラー 1** | マスタードイエロー | `#E9C46A` | おすすめバッジ、星評価（★）、ハイライト |
| **アクセントカラー 2** | セージグリーン | `#2A9D8F` | マップピン、現在地、カテゴリタグ（自然・発見） |

### 2.2 フォント設定
Google Fonts を利用した温かみと可読性を両立するフォント構成です。

* **見出し（タイトル・店名）**: `Zen Maru Gothic`
* **本文（説明文・口コミ・UI）**: `Noto Sans JP`
* **CSS指定例**:
  ```scss
  $font-heading: 'Zen Maru Gothic', sans-serif;
  $font-base: 'Noto Sans JP', sans-serif;

  body {
    font-family: $font-base;
    color: #2B2D42;
    background-color: #FAF7F2;
  }
  ```

### 2.3 余白・角丸・ブレイクポイント基準
* **余白（Spacing）**: 8px / 4px グリッド
  * `4px` (xs), `8px` (sm), `16px` (md / 標準), `24px` (lg), `32px` (xl), `48px` (2xl)
* **角丸（Border Radius）**:
  * カード / モーダル: `16px`
  * ボタン / フォーム入力: `8px` 〜 `12px`
  * タグ / バッジ: `9999px`（完全な角丸・ピル型）
* **ブレイクポイント（レスポンシブ）**:
  * スマホ（基準）: `〜 639px`
  * タブレット: `640px 〜` (`sm`)
  * PC: `768px 〜` (`md`) / `1024px 〜` (`lg`)
  * ※ PC画面で閲覧された場合も、中央にスマホ幅コンテナ（最大幅 480px〜640px 程度）を配置してスマホ体験を維持することを推奨。

---

## 3. 技術スタック & アセット管理

* **スタイリング**: **Sass (SCSS)**
  * 変数・ネスト・ミックスインを活用して共通化・構造化を行う。
* **スクリプト**: **素のJavaScript (Vanilla JS)**
  * ライブラリに依存せず、軽量でシンプルなDOM操作・機能実装を行う。
* **画像アセット運用**:
  * **画像形式**: 写真・サムネイル等は **WebP** を優先使用。UIアイコンは **SVG** を使用。
  * **配置ディレクトリ構成**:
    ```text
    assets/images/
    ├── common/    # ロゴ、共通UIパーツ、デフォルトアバター
    ├── icons/     # SVGアイコン素材
    └── shops/     # 店舗サムネイル、料理、内観などのサンプル画像
    ```

---

## 4. コーディング規約

### 4.1 HTML / SCSS クラス命名規則（BEM記法）
プレフィックスを用いた BEM（Block-Element-Modifier）記法を徹底し、クラス名の衝突を防ぎます。

* **プレフィックス**:
  * `c-` : コンポーネント（例: `c-shop_card`, `c-btn`）
  * `p-` : ページ固有ブロック（例: `p-home_header`, `p-shop_detail`）
  * `l-` : レイアウト（例: `l-container`, `l-header`）
  * `u-` : ユーティリティ（例: `u-text_center`, `u-mb_16`）
  * `is-` / `has-` : 状態（例: `is-active`, `is-hidden`）
* **命名例**:
  ```html
  <article class="c-shop_card">
    <img class="c-shop_card__thumbnail" src="assets/images/shops/sample.webp" alt="カフェ外観">
    <div class="c-shop_card__body">
      <h3 class="c-shop_card__title">路地裏珈琲店</h3>
      <span class="c-shop_card__tag c-shop_card__tag--popular">地元民イチオシ</span>
    </div>
  </article>
  ```

### 4.2 インデント & フォーマット
* **インデント**: **半角スペース 2つ（Soft Tabs: 2）**
* **文字コード**: `UTF-8`
* **改行コード**: `LF`

### 4.3 ファイル・フォルダの命名規則
* **スネークケース（snake_case）** で統一します（すべて小文字・単語間はアンダースコア `_`）。
* **例**:
  * HTML: `index.html`, `shop_detail.html`, `post_review.html`
  * SCSS: `_shop_card.scss`, `style.scss`, `_variables.scss`
  * JS: `main.js`, `shop_detail.js`, `map_view.js`
  * 画像: `shop_sample_01.webp`, `icon_heart.svg`

### 4.4 コミットメッセージ規約
Conventional Commits に準拠したプレフィックスと簡潔な日本語で記述します。

* **フォーマット**: `<プレフィックス>: <変更内容の要約>`
* **プレフィックス一覧**:
  * `feat:` 新機能・新規画面の作成
  * `fix:` バグや表示崩れの修正
  * `style:` CSS/SCSSの調整、余白・配色の変更（動作に影響しない修正）
  * `refactor:` コードの整理・構造改善
  * `docs:` ドキュメント（README / AGENTS.md等）の追加・修正
  * `chore:` 画像などのアセット追加、ビルド・環境設定の更新
* **コミット例**:
  * `feat: 店舗詳細画面のレイアウト作成`
  * `fix: スマホ幅でのヘッダーメニューの重なりを修正`
  * `style: ショップカードの角丸と影の調整`

---

## 5. 検証・品質チェック手順

作業完了後、コミットやプルリクエストを作成する前に必ず以下の手順を実施してください。

1. **ビルド & SCSSコンパイルチェック**
   ```bash
   npm run build
   ```
   * SCSSの文法エラーやアセットの参照切れがなく、正常にビルドが通ることを確認する。

2. **ブラウザ動作 & スマホ表示確認**
   * ブラウザの開発者ツールを開き、**デバイスモード（幅 375px〜430px）** で以下の項目を確認する。
     * [ ] 意図しない横スクロール（コンテンツの横はみ出し）が発生していないか
     * [ ] ボタンやリンクがタップしやすいサイズ（最小 44x44px 目安）になっているか
     * [ ] ブラウザの Console タブに JavaScript のエラーや未解決の警告が出ていないか
